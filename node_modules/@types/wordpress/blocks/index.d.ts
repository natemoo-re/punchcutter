export { }

declare global {
    
    namespace wp {
        const blocks: Blocks;
    }

    interface Blocks {
        registerBlockType: (name: string, typeDefinition: BlockTypeDefinition) => void;
    }

    interface BlockTypeDefinition {
        title: string;
        icon: string;
    }

}
