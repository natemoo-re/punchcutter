import {
    BlockDecorator, BlockOptions,
    AttributeDecorator, AttributeOptions
} from "../../src/declarations/decorators";

export const Block: BlockDecorator = (opts: BlockOptions) => null;

export const Attribute: AttributeDecorator = (opts: AttributeOptions) => null;